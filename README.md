# bankFerry
Uses a financial data source (e.g. Plaid) to download transactions into OFX files for import into GnuCash, with payee rewriting support.  Not for the faint of heart, as setup requires LetsEncrypt, DynDNS, and HAProxy setups to bind accounts.
